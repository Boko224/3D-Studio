README.md

# 3D Print Studio MVP

Модерна React приложение за 3D печатен бизнес с магазин и услуга за качване на модели.

## Функционалности

✅ **Магазин** - Бърз преглед на продукти с филтриране по категория
✅ **Персонализация** - Текст, цветове, материали с динамично ценообразуване
✅ **Качване на модели** - Drag & drop зона за .STL, .OBJ, .GCODE файлове
✅ **Кошница** - LocalStorage и Firebase базирана кошница с управление на количества
✅ **Чекаут** - Форма за доставка и избор на куриер
✅ **Email уведомления** - Автоматични имейли за потвърждение на поръчки
✅ **Автентикация** - Firebase Authentication с потребителски профили
✅ **Admin панел** - Управление на продукти и поръчки
✅ **Потребителски профил** - История на поръчки и лични данни
✅ **Responsive Design** - Идеално работи на мобилни и desktop устройства

## Tech Stack

- **Frontend**: React 18 + Vite
- **Styling**: Tailwind CSS
- **Icons**: Lucide React
- **State Management**: React Context API
- **Routing**: React Router v6
- **Backend/Database**: Firebase (Firestore, Authentication, Storage)
- **Storage**: LocalStorage + Firebase (за кошница и поръчки)
- **Hosting**: Firebase Hosting

## Началось

### Инсталация

```bash
cd 3D-sait
npm install
```

### Развитие

```bash
npm run dev
```

Приложението ще се отвори на `http://localhost:5173`

### Производство

```bash
npm run build
```
## Deploy to Firebase Hosting

След като приложението е готово за production, можеш лесно да го разположиш в Firebase Hosting.

Преди това се увери, че имаш инсталирани Node.js/NPM и че проекта ти в Firebase е създаден (имаш `projectId` — в нашия случай `d-print-studio`).

Стъпки (PowerShell):

1) Инсталирай Firebase CLI (само веднъж):

```powershell
npm install -g firebase-tools
```

2) Логни се в Firebase (интерактивно, ще отвори браузър):

```powershell
firebase login
```

3) Избери/свържи проекта с локалното репо (ако още не си го инициализирал):

```powershell
firebase use --add
# Следвай интерактивния избор и избери 'd-print-studio' или въведи projectId
```

4) Инициализирай Hosting (първо пускане - ще те попита кои услуги; избери Hosting):

```powershell
firebase init hosting
```

Важно: При въпроса за `public directory` напиши `dist` (Vite output directory). За SPA (single-page app) отговори `Yes` на "Configure as a single-page app (rewrite all urls to /index.html)?".

5) Създай билд на приложението (Vite):

```powershell
npm run build
```

6) Деплой в Firebase Hosting:

```powershell
firebase deploy --only hosting
```

След успешен деплой ще получиш URL като `https://<your-site>.web.app` и `https://<your-site>.firebaseapp.com`.

Бърз preview (preview channel):

```powershell
# Създава preview канал (полезно за тестове):
firebase hosting:channel:deploy staging --expires 7d
# Отваря preview URL в браузър:
firebase hosting:channel:open staging
```

Локално тестване на готовия `dist` (алтернативен бърз preview без Firebase):

```powershell
# Инсталирай http-server ако нямаш:
npm install -g http-server
# Стартирай в dist:
http-server ./dist -p 5000
# Отвори: http://localhost:5000
```

Бележки и troubleshooting:
- Увери се, че `public directory` е зададен на `dist` при `firebase init hosting`.
- Ако имаш проблем с правата за Firestore/Storage (permission denied), отвори Firebase Console → Firestore / Storage → Rules и временно постави тестови правила (само за разработка). Не оставяй такива правила в production.
- Ако имаш няколко Firebase проекта, използвай `firebase use <alias>` или `firebase use --add` за да свържеш правилния проект.
- `src/config/firebase.js` трябва да съдържа правилните Firebase credentials за проекта; обикновено не е нужно да го променяш при хостване.

## Email уведомления (EmailJS)

Приложението използва EmailJS за изпращане на автоматични имейли при поръчки.

**За да активираш имейл уведомленията:**

1. Прегледай детайлните инструкции в [EMAILJS_SETUP.md](EMAILJS_SETUP.md)
2. Създай безплатен акаунт в [EmailJS](https://www.emailjs.com/)
3. Конфигурирай Email Service и Template
4. Обнови credentials в `src/services/emailService.js`

Без конфигурация, системата работи в **demo режим** - поръчките се записват нормално, но имейлите не се изпращат (само се логват в конзолата).

## Структура на проекта

```
src/
├── components/        # Преизползвани UI компоненти
│   ├── Button.jsx
│   ├── Footer.jsx
│   ├── Input.jsx
│   ├── Navbar.jsx
│   └── ProductCard.jsx
├── context/          # React Context за управление на състояние
│   ├── CartContext.jsx
│   └── UserContext.jsx
├── config/           # Конфигурация
│   └── firebase.js   # Firebase настройки
├── services/         # Услуги и API интеграции
│   └── emailService.js  # EmailJS интеграция
├── data/             # Примерни данни (продукти, материали)
│   └── products.js
├── pages/            # Страни на приложението
│   ├── Home.jsx
│   ├── Shop.jsx
│   ├── ProductDetail.jsx
│   ├── Cart.jsx
│   ├── Upload.jsx
│   ├── About.jsx
│   ├── Auth.jsx      # Вход/Регистрация
│   ├── Profile.jsx   # Потребителски профил
│   └── Admin.jsx     # Административен панел
├── App.jsx           # Главното приложение
├── main.jsx          # Входна точка
└── index.css         # Глобални стилове
```

## Страни

- **Home** (`/`) - Начална страница с hero section и популярни продукти
- **Shop** (`/shop`) - Каталог на продукти с филтриране по категория
- **Product Detail** (`/product/:id`) - Детайли на продукт със персонализация
- **Cart** (`/cart`) - Кошница и чекаут процес
- **Upload** (`/upload`) - Качване на 3D модели за оценяване
- **About** (`/about`) - Информация за компанията и услугите
- **Auth** (`/auth`) - Страница за вход и регистрация на потребители
- **Profile** (`/profile`) - Потребителски профил с история на поръчки
- **Admin** (`/admin`) - Административен панел за управление (защитен)

## Данни

### Продукти

Пример на продукт (в `src/data/products.js`):

```json
{
  "id": "keychain-001",
  "name": "Ключодържател с име",
  "basePrice": 12.00,
  "category": "keychains",
  "image": "🔑",
  "options": {
    "colors": ["Черен", "Бял", "Червен", "Син"],
    "materials": ["PLA", "PETG"]
  },
  "customizable": true,
  "description": "Персонализиран ключодържател..."
}
```

## Функции

### Кошница (CartContext)

- Добави/премахни продукти
- Управление на количества
- Динамичен изчисляване на цена
- LocalStorage синхронизация

### Персонализация

- Вход на текст (макс. 15 символа)
- Избор на цвят
- Избор на материал (PLA / PETG +2 лв.)
- Динамично ценообразуване

### Качване на файлове

- Drag & drop функционалност
- Валидация на файловия тип (.STL, .OBJ, .GCODE)
- Проверка на размера (макс. 50MB)
- Форма за контакт

## Чекаут

- Избор на куриер (Econt, Speedy, Локално теглене)
- Форма за доставка (име, адрес, град)
- Итоговая цена (продукти + доставка)
- Успешно подадена поръчка

## Бъдещи подобрения

- 🔄 Интеграция с реално плащане (Stripe, PayPal)
- 🔄 Галерия на предишни проекти с реални снимки
- 🔄 Рейтинги и отзиви от клиенти
- 🔄 Автоматично изчисляване на цена на 3D модели по размер и обем
- 🔄 Push уведомления за статус на поръчка
- 🔄 Интеграция с куриерски API за реално проследяване

## Лицензия

Copyright © 2025 3D Print Studio BG. Всички права запазени.
